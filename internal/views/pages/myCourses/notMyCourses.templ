package myCourses

import (
	"fmt"
	"github.com/jonahhess/ds/internal/types"
)

templ NotMyCourses(userID int, courses []types.Item) {
	<h1>All Available Courses</h1>
	<ul>
		for _, v := range courses {
			<li>
				<a href={ fmt.Sprintf("/courses/%d", v.ID) }>{ v.Text }</a>
			</li>
		}
	</ul>
}
