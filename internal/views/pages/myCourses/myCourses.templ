package myCourses

import (
	"fmt"
	"myapp/internal/types"
)

templ MyCourses(userID int, courses []types.Item) {
	<h1>All My Courses</h1>
	for _, v := range courses {
		<a href={ fmt.Sprintf("/users/%d/courses/%d", userID, v.ID) }>{ v.Text }</a>
	}
}
