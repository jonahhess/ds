package template

import (
	"myapp/types"
	"myapp/utils"
)

templ Template() {
	switch utils.GetUserTypeFromContext(ctx) {
		case types.Guest:
			<h1>Template Guest</h1>
			<a href="/login">Login</a>
		case types.LoggedInUser:
			<h1>Template User</h1>
	}
}
