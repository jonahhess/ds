package courses

import (
	"database/sql"
	"myapp/utils"
)

templ Courses(DB *sql.DB) {
	if utils.IsLoggedIn(ctx) {
		<h1>All Courses</h1>
		{{ GetAllCourses(ctx, DB) }}
	} else {
		<h1>Please log in to continue</h1>
	}
}
