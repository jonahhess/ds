package myCourses

import (
	"fmt"
	"myapp/types"
	"myapp/utils"
)

templ MyCourses(courses []types.Item) {
	<h1>All My Courses</h1>
	for _, v := range courses {
		<a href={ utils.UserLink(ctx, fmt.Sprintf("courses/%d", v.ID)) }>{ v.Text }</a>
	}
}
