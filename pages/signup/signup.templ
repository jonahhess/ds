package signup

import (
	"myapp/types"
	"myapp/utils"
)

templ Signup() {
	switch utils.GetUserTypeFromContext(ctx) {
		case types.Guest:
			<h1>Signup Guest</h1>
			<a href="/login">Login</a>
		case types.LoggedInUser:
			<h1>Signup User</h1>
	}
}
