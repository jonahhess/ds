package home

import (
	"myapp/types"
	utils "myapp/utils"
	)

templ Home() {
	switch utils.GetUserTypeFromContext(ctx) {
		case types.Guest:
			<h1>Hello Guest</h1>
			<a href="/login">Login</a>
		case types.LoggedInUser:
			<h1>Hello User</h1>
	}
}
