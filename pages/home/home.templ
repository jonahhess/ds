package pages

import "myapp/types"

templ Home(user types.UserType) {
	switch user {
		case types.Guest:
			<h1>Hello Guest</h1>
			<a href="/login">Login</a>
		case types.UserType(1):
			<h1>Hello User 1</h1>
	}
}
